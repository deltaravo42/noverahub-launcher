# =============================================================================
# Novera Hub Launcher – environment (what the launcher actually uses)
# =============================================================================
# The launcher only reads HUB_URL and GITHUB_REPO. It does NOT use Turso,
# Pusher, R2, or NEXTAUTH — those are used by the Novera Hub web app (your
# backend). The launcher is "connected" to your database and services through
# HUB_URL: when users log in or launch, the launcher talks to the Hub at
# HUB_URL, and the Hub uses Turso, Pusher, R2, etc.
# =============================================================================

# Novera Hub URL. Launcher connects here for login, launch, and content (notices/events/news).
# Use the main domain (e.g. https://noverahub.com). The Hub uses your Turso, Pusher, R2, etc.
HUB_URL=https://noverahub.com

# GitHub repo for "Check for updates" (owner/repo). Public API only — no token needed.
GITHUB_REPO=deltaravo42/noverahub-launcher

# -----------------------------------------------------------------------------
# Optional: if this file is shared with the main Novera Hub repo, you can keep
# Turso, Pusher, R2, NEXTAUTH, etc. here for the Hub app. The launcher ignores
# them and only uses HUB_URL and GITHUB_REPO above.
# -----------------------------------------------------------------------------
